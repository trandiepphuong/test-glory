export type SignIn = {
    phone: string;
    password: string;
};

export type SignUp = {
    firstName: string;
    lastName: string;
    phone: string;
    password: string;
    confirmPassword: string;
};
